# Anti-Circularity Audit

| Potential circle or misreading | Response |
|---|---|
| Critical-neutral amplitude assumes RH | It fixes the positive endpoint image; universal occupation is not assumed. |
| Explicit formula smuggles RH | The explicit formula supplies route projection, not zero-location. |
| Off-line coordinates are declared illegal | Coordinate and support-level reports are preserved. |
| Non-neutral amplitude is banned as analytic data | It is banned only as endpoint-standing separator authority. |
| A+ does analytic work | A+ excludes an independent endpoint discriminator after endpoint use; it does not derive amplitude exactness. |
| Arbitrary negation becomes separator | Anti-trivialization requires target, route, projection-role content, counterforce, and exhaustion. |
| Local reductio adds a premise | The annotation is a proof-act role, not an object-level conjunction. |
| Pairing or cancellation ignored | They are audited; they become bridge if they prove endpoint-neutral occupation. |
| Corpus support imports analytic facts | The corpus ledger supplies structural constraints only. |

## Structural-to-EF bridge anti-circularity additions

- The EF bridge is not defined as line support. It records route, same-zero, report-level, and ATS trajectory conditions.
- The order invariant is recoverable from the critical-normalized EF contribution, not merely a renamed coordinate.
- The bridge fork keeps three branches open: bridge-complete neutralization, domain shift, or independent discriminator.
- The earlier structural RH paper's bridge-completion framing is preserved: this manuscript instantiates the bridge at the EF amplitude order interface.


## Native Counterforce Anti-Circularity Addendum

Potential circle: non-neutral order is excluded merely because it is negative.

Audit response: the new section explicitly preserves non-neutral order as native analytic content. It is excluded only after it is used with endpoint-standing counterforce on the fixed RH-EF carrier and only after role exhaustion removes bridge-neutralization, support-only, bookkeeping, and domain/interface shift.

Potential circle: ordinary counterexample force is relabeled as forbidden.

Audit response: ordinary native falsification is not granted a fifth role; it must either be a bridge-neutralized endpoint act, support/bookkeeping, domain shift, or independent endpoint-status discrimination. This is a role-exhaustion theorem, not a raw-negation rule.


## Counterexample-force anti-circularity addendum

| Potential circle or misreading | Response |
|---|---|
| Every counterexample is forbidden | False. Bare counterexample content and ordinary counterexample theoremhood are lawful. Only official counterexample force or local exact-complement counterforce enters endpoint-use discipline. |
| Objects are acts | False. The object or zero is raw content; the act of offering it as endpoint-defeating counterexample is endpoint use. |
| Analytic falsification is banned | No. The manuscript classifies endpoint-standing falsification as endpoint use and then applies the same endpoint discipline that governs official resolution. |
| The proof proves arbitrary propositions | No. Arbitrary negation does not supply official counterexample force, official route projection, endpoint-counterforce use, or amplitude endpoint-status work. |

## No-autonomous-counterexample addendum

| Potential circle or misreading | Response |
|---|---|
| The proof hides the premise that counterexamples are illegal | False. Counterexample content is lawful. Only official endpoint-defeating use is endpoint use. |
| There is an autonomous ordinary-negative endpoint role | No. A branch with official endpoint force is endpoint use; without endpoint use it has no official counterexample force. |
| The proof rejects ordinary falsification by stipulation | It rejects only the fifth role: settled same-carrier endpoint-defeating force while refusing endpoint-use status. |

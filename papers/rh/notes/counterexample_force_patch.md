# Counterexample Force as Endpoint Use Patch

This patch addresses the final hostile-referee objection: that an off-line zero used against RH is merely an ordinary mathematical counterexample, not endpoint governance.

## Added load-bearing material

- New early section: **Counterexample Force as Endpoint Use**.
- Four counterexample layers: bare counterexample content, ordinary counterexample theoremhood, official counterexample force, and local counterexample force.
- Theorem: bare counterexample content is not endpoint use.
- Theorem: ordinary counterexample theoremhood is kernel-internal and not automatically discriminatory.
- Theorem: official counterexample force is endpoint use.
- Theorem: official counterexample force satisfies target adequacy and triggers the kernel/A+ consequence layer.
- Theorem: local exact-complement counterexample force is local endpoint use.
- Theorem: local endpoint use triggers local A+ closure.
- RH-EF specialization: off-line counterexample force projects to EF non-neutral order and enters native-counterforce exhaustion.
- Corollary: no separate ordinary-counterexample escape role remains under live endpoint counterforce.

## Purpose

The patch distinguishes raw object/content from act-level endpoint use. The manuscript does not classify the zero, coordinate, or formula as endpoint use. It classifies the act of offering the off-line branch as counterexample force against the official RH-EF endpoint. That act fixes target, carrier, branch, reportability, downstream reuse, branch exclusion, act-time finality, and lawful-redescription-stable endpoint role; therefore it is endpoint use.

## No-autonomous-counterexample role upgrade

This upgrade names the final commitment directly: there is no same-carrier official counterexample force outside endpoint-use discipline. The corollary rules out `OfficialCounterexampleForce(B) and not EndpointUse(B)`, and the collapse theorem states that a negative branch without endpoint-use status cannot settle the official endpoint.

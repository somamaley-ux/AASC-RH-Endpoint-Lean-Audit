# RH hardening patch

This edition implements the cross-paper hardening plan for the RH prime-trace amplitude separator manuscript.

## Main manuscript additions

- Added a claim-status ledger distinguishing carrier/context, zerohood standing, positive critical-neutral endpoint image, raw negative content, explicit-formula support, deployed native off-line falsification, surrogate analytic programs, and Lean audit support.
- Added a no-one-step-retyping lock in the proof spine: the manuscript does not infer `Damp(rho)` directly from `not Lcrit(rho)`, `beta != 1/2`, or `x^(beta-1/2) != x^0`.
- Added the local RH-EF kernel packet K1-K13, making the A+ consequence surface explicit in zeta/prime-trace vocabulary.
- Added the theorem that non-neutral amplitude is not a lawful coequal RH-EF endpoint role; the broad outcome ledger is not the critical-neutral amplitude endpoint image.
- Expanded the native-counterforce section with the Hodge-style `O_nat(rho)` package and the full ordinary native off-line falsification collapse.
- Replaced the compact weakening section with K5/K6/K11/K13 weakening resistance for the local RH-EF endpoint packet.
- Added a Poincare-style route-locus collapse audit covering coordinate report, EF support, pairing/cancellation, smoothing/truncation, spectral surrogate, positivity criterion, numerical verification, density estimates, partial zero-free regions, carrier/interface shift, repair, endpoint classifier, and hidden fifth/sixth native roles.
- Updated the denial-burden and faithful-counterexample worksheets with the new attack surfaces.
- Added a Lean appendix boundary note clarifying that the hardening material is manuscript-level defensive expansion of already represented endpoint-route roles.

## Build status

The project builds with `./build.sh` and produces synced PDFs in `build/`.

## Final pairing/cancellation patch

The final polish pass adds a named pairing/cancellation endpoint-role theorem.  Pairing, conjugate symmetry, smoothing cancellation, and aggregate trace cancellation are preserved as analytic mathematics, but they do not form an autonomous endpoint role.  If cancellation is endpoint-decisive on the fixed RH-EF carrier, it is support, bridge-complete neutralization, bookkeeping, carrier/interface shift, or `Damp`-inducing endpoint-status work.  The route-locus audit, denial-burden worksheet, hostile-referee matrix, README, and Lean-boundary note were updated to reflect this closure.

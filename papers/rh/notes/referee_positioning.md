# Referee Positioning

This manuscript is written in a hostile-referee posture. The intended attack point is not whether off-line amplitude is native analytic data; that is granted. The attack point is whether endpoint-standing use of non-neutral amplitude is independent same-domain endpoint-status work. The manuscript therefore separates descriptive data, support data, projection-role content, endpoint counterforce, separator occupation, and discriminator closure.


## Counterexample-force positioning

This edition moves the final hostile dispute to act-level endpoint use. A raw off-line zero is allowed as content. Ordinary theoremhood of a negative branch is allowed as theoremhood. The manuscript claims that when the off-line branch is offered as a same-carrier counterexample force against the official RH-EF endpoint, the act fixes the endpoint-use features and therefore falls under kernel/A+/ATS/UEAP discipline.

## Final denial fork

After the no-autonomous-counterexample patch, a hostile same-mode referee must either break the theorem that official counterexample force is endpoint use, break endpoint-use-to-A+ closure, break the EF projection/order bridge, break native-counterforce exhaustion, or produce a genuine autonomous ordinary-negative endpoint role.

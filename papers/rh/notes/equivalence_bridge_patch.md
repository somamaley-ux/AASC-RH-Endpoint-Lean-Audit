# Equivalence bridge patch notes

This patch adds the structural-to-explicit-formula equivalence bridge requested after comparison with the P = NP endpoint machinery and the structural RH paper.

The bridge maps structural roles to explicit-formula roles: zerohood -> same-zero EF contribution, line readout -> neutral critical-normalized order, off-line readout -> non-neutral order. It defines the EF bridge object as route/identity/report/trajectory preservation, not line support. It then replaces the old bare hinge with a bridge fork: bridge completion, domain shift, or amplitude discriminator.
